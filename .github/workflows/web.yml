name: Web Tests

on:
  push:
    paths:
      - 'web-tests/**'
  pull_request:
    paths:
      - 'web-tests/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configurar Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Rodar testes Web
        run: mvn test -pl web-tests -Dheadless=true

      - name: Gerar relatorio Allure
        if: always()
        run: mvn -pl web-tests allure:report

      - name: Publicar relatorio Allure
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-web
          path: web-tests/target/site/allure-maven-plugin/

      - name: Exibir link do relatorio
        if: always()
        run: |
          echo "## Relatorio do Web" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Versao mais atualizada (publicada a cada push na main):" >> "$GITHUB_STEP_SUMMARY"
          echo "https://marcelojunior1604.github.io/qa-teste-tecnico/web/index.html" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "O artefato **allure-report-web** (aba Artifacts acima) tem o relatorio bruto desta execucao especifica, mas precisa ser servido por HTTP pra abrir (o Allure nao funciona com duplo clique direto no arquivo)." >> "$GITHUB_STEP_SUMMARY"